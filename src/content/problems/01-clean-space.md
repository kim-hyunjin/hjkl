---
title: "줄 끝 공백 제거"
summary: "여러 파일에서 줄 끝 공백을 한 번에 정리합니다."
order: 1
difficulty: "중급"
tags: ["정리", "치환"]
practice: true
practiceFile: "example01.md"
---

## 상황

코드 리뷰에서 "줄 끝 공백"을 지적받았습니다. 여러 파일의 공백을 하나씩 지우는 것은 비효율적입니다.

## 해결

```
:%s/\s\+$//g
```

줄 끝의 공백을 모두 제거합니다.

파일을 여러 개 열어놨다면 아래처럼 한 번에 적용합니다.

```
:argdo %s/\s\+$//g | update
```

- `:argdo` : 인자 목록(argument list)의 각 파일에 실행
- `| update` : 변경된 파일 저장

## 절차

1. `vim *.js`로 여러 파일 열기
2. `:argdo %s/\s\+$//g | update`
3. `:qall`로 모두 종료

## 결과 확인

```
:set list            → 공백 표시(^$ 등)
:set listchars=trail:·
```

`trail` 설정으로 줄 끝 공백이 보이게 할 수 있습니다. 정리 후 사라졌는지 확인하세요.

## 연습

1. 예제 파일에서 줄 끝에 공백을 몇 개 추가
2. `:%s/\s\+$//g`로 제거
3. `:set list`로 확인

> 아래 실습에서 예제 파일을 열고 직접 해볼 수 있습니다.
